# Netlify config for a single static HTML site

[build]
  command = "node scripts/generate-config.mjs"
  publish = "."

[[headers]]
  for = "/*"

  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    Referrer-Policy = "no-referrer"
